import os
import time
from pathlib import Path
from dotenv import load_dotenv
from groq import Groq
from pydantic import BaseModel, Field

load_dotenv()
my_api_key=os.getenv("GROQ_API_KEY")

if not my_api_key:
    raise ValueError("API key kaha hai bhai")

client=Groq(api_key=my_api_key)
model = "openai/gpt-oss-120b"


job_description = """
Job Title: R&D Engineer – AI & Emerging Technologies
Position: Trainee / Associate
Experience: 0–1 Years
Employment Type: Full-Time

About the Role:

We are looking for a curious, technically confident, and self-driven R&D Engineer
to join our Research & Development team. This role is designed for freshers with
strong programming fundamentals and an interest in emerging technologies.

The role involves exploring AI, LLMs, automation, developer tools, backend
technologies, and other emerging technologies. The candidate should be able to
identify problems, evaluate technical approaches, build proof-of-concepts,
experiment with technologies, and present technical solutions.

Roles & Responsibilities:

- Explore emerging technologies, frameworks, libraries, and tools.
- Identify opportunities for innovation, engineering improvement, and automation.
- Evaluate different technical solutions and approaches.
- Design and develop proof-of-concepts, prototypes, and experimental applications.
- Work with AI/ML technologies including LLM APIs, prompt engineering, RAG
  pipelines, vector databases, agentic workflows, and AI-assisted development.
- Research and benchmark solutions based on performance, scalability, cost,
  security, reliability, and maintainability.
- Develop internal tools, scripts, and automation to improve developer productivity.
- Debug experimental systems and refine solutions through iterative experimentation.
- Document research findings, implementation decisions, limitations, and
  recommendations.
- Present technical demonstrations and explain technical decisions and trade-offs.
- Stay informed about industry developments, technical publications,
  open-source projects, and emerging engineering practices.
- Collaborate with engineers, architects, and product teams.

Required Skills & Qualifications:

- Bachelor's degree in Computer Science, Information Technology, or a related
  engineering discipline.
- Strong programming fundamentals including:
  Data Structures, Algorithms, Object-Oriented Programming, and Problem Solving.
- Hands-on coding proficiency in at least one of:
  Python, JavaScript/TypeScript, Ruby, Java, or Go.
- Understanding of web fundamentals including:
  HTTP, REST APIs, JSON, and client-server architecture.
- Familiarity with relational databases and basic SQL.
- Working knowledge of Git and version control.
- Strong analytical and debugging skills.
- Ability to independently learn new technologies through documentation,
  experimentation, and practical implementations.
- Strong technical curiosity and interest in understanding how technologies work.
- Ability to take initiative and ownership of technical experiments.
- Clear written and verbal communication skills.

Good to Have:

- Personal projects and GitHub repositories.
- Open-source contributions.
- Exposure to AI/ML concepts.
- Experience with LLM APIs such as OpenAI or Anthropic.
- Experience with LangChain and LangGraph.
- Knowledge of embeddings and vector databases.
- Experience using AI coding assistants.
- Familiarity with cloud platforms such as AWS, GCP, or Azure.
- Experience with Docker and containerization.
- Backend experience with FastAPI, Flask, Rails, Express, or Spring Boot.
- Frontend experience with React.js or Vue.js.
- Awareness of system design, caching, messaging queues, and application
  architecture.
- Experience building scripts, utilities, or automation.
- Participation in hackathons, internships, research projects, or
  competitive programming.

Ideal Candidate:

The candidate should enjoy solving challenging technical problems, exploring
how technologies work, building practical solutions, experimenting with new
technologies, and working independently on unfamiliar problems.

The role values practical implementation, technical curiosity, problem-solving,
experimentation, and the ability to turn ideas into working prototypes.
"""
class JobD(BaseModel):
    role: str
    required_skills: list[str]
    preferred_skills: list[str]
    minimum_experience: float | None
    education_requirements: list[str]
    responsibilities: list[str]

jobd_schema = JobD.model_json_schema()

system_prompt = f"""
You are an expert HR assistant.

Your job is to analyze job descriptions and extract
structured information from them.

Return ONLY valid JSON matching this schema:

{jobd_schema}
IMPORTANT:
Do NOT return the schema itself.
Do NOT return fields like "properties", "title" or "type".
Fill the schema with actual information extracted from the job description.

If minimum experience is not mentioned, return null.
If information for a list is missing, return an empty list.
Do not invent information.
"""

user_prompt = f"""
Analyze the following job description:

{job_description}
"""
message_system={
    "role" : "system",
    "content" : system_prompt
}
message_user={
    "role" : "user",
    "content" : user_prompt
}
response_format={
    "type" : "json_object"
}


messages=[message_system, message_user]

response=client.chat.completions.create(model=model, messages=messages, response_format=response_format)


answer=response.choices[0].message.content

raw_json=answer
# print(raw_json)



import json
job_data=json.loads(raw_json)

job = JobD(**job_data)

print(job.minimum_experience)
print(job.education_requirements)



#parse real
class MatchResult(BaseModel):
    score: float
    details: dict
class Experience(BaseModel):
    company: str | None = None
    role: str | None = None
    duration: str | None = None
    description: str | None = None
    skills_used: list[str] = []

class Resume(BaseModel):
    name: str | None = None
    email: str | None = None
    phone: str | None = None

    total_experience_years: float | None = None

    skills: list[str] = []
    experiences: list[Experience] = []
    education: list[str] = []
    projects: list[str] = []
    certifications: list[str] = []


resume_schema = Resume.model_json_schema()
def final_score(job,resume):
    match_schema = MatchResult.model_json_schema()
    prompt = f"""
    You are an HR recruiter.

    Compare the candidate's resume with the job description.

    JOB DESCRIPTION:
    {job.model_dump_json(indent=2)}

    CANDIDATE RESUME:
    {resume.model_dump_json(indent=2)}
    Return JSON matching this schema:

    {match_schema}

    Give me:

    1. Candidate name
    2. Matching skills
    3. Missing important skills
    4. Whether experience requirement is met
    5. Overall match percentage from 0 to 100
    6. A short final verdict

    Keep the response concise and easy to read.
    """
    message={
        "role": "user",
        "content" : prompt
    }
    messages=[message]
    response_format={
        "type": "json_object"
    }
    response = client.chat.completions.create(model=model, messages=messages, response_format=response_format)
    data = json.loads(response.choices[0].message.content)
    return MatchResult(**data)
def parse_resume(resume_text):
    system_prompt = f"""
    You are an expert resume parser.

    Extract information from the resume based on its meaning,
    not only based on exact section headings.

    Different resumes may use different headings.

    For example:
    - Experience
    - Professional Experience
    - Work History
    - Employment
    - Internships

    These may all contain relevant experience.

    Skills may also appear in the skills section, work experience,
    internships or projects.

    Return ONLY valid JSON matching this schema:

    {resume_schema}

    Important rules:

    1. Do not invent information.
    2. If a value is not available, return null.
    3. If a list has no information, return an empty list.
    4. Include internships inside experiences.
    5. Extract skills mentioned across the entire resume.
    """
    user_prompt = f"""
    Parse the following resume:

    {resume_text}
    """
    message_system={
        "role" : "system",
        "content" : system_prompt
    }
    message_user={
        "role" : "user",
        "content" : user_prompt
    }
    messages=[message_system, message_user]
    response_format={
        "type": "json_object"
    }
    response=client.chat.completions.create(model=model, messages=messages, response_format=response_format)
    raw_output = response.choices[0].message.content
    data = json.loads(raw_output)
    resume = Resume(**data)
    return resume


from pypdf import PdfReader
from docx import Document
def read_pdf(file_path):
    reader = PdfReader(file_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text

def read_docx(file_path):
    document = Document(file_path)
    text = ""
    for paragraph in document.paragraphs:
        if paragraph.text.strip():
            text += paragraph.text + "\n"
    
    for table in document.tables:
        for row in table.rows:
            for cell in row.cells:
                if cell.text.strip():
                    text += cell.text + "\n"
    return text


def read_resume(file_path):
    if file_path.suffix.lower() == ".pdf":
        return read_pdf(file_path)
    elif file_path.suffix.lower() == ".docx":
        return read_docx(file_path)
    else:
        return None



# lets do it now
resume_folder = Path("resumes")
all_results=[]
for file_path in resume_folder.iterdir():
    #C:\Users\Pratyush\padho_with_pratyush\week1\day5\resumes\abhay resume new - Abhay Singh.pdf
    if file_path.suffix.lower() not in [".pdf", ".docx"]:
        continue
    print("\nProcessing:", file_path.name)
    resume_text = read_resume(file_path)
    parsed_resume=parse_resume(resume_text) # llm call1
    time.sleep(5)
    result = final_score(job, parsed_resume) #llm caLL2
    #score and details
    #acount chtgpt
    # request bhejna shhur krega millions
    #chattgot server jam ho jayega
    time.sleep(5)
    print("Score:", result.score)
    all_results.append({
        "name": parsed_resume.name,
        "score": result.score,
        "details": result.details
    })
all_results.sort(
    key=lambda candidate: candidate["score"],
    reverse=True
)
top_2 = all_results[:2]
worst_2 = all_results[-2:]


print("TOP 2 CANDIDATES")
for candidate in top_2:

    print(
        candidate["name"],
        "-",
        candidate["score"],
        "%"
    )

    print(candidate["details"])

print("LOWEST 2 CANDIDATES")
for candidate in worst_2:

    print(
        candidate["name"],
        "-",
        candidate["score"],
        "%"
    )
    print(candidate["details"])